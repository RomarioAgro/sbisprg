# обработчик выборки 'Расход'
Функция 'Расход'(Событие)
{
   ВыборПо(Событие)
   {
      выбор "Инициализация":
         Вернуть 0;
      выбор "Расчет":
         @Страна=Есть(СТРАНА)?СТРАНА:"";
         Вернуть 0;
      выбор "Создание":
         Вернуть 0;
      выбор "Замена":
         Вернуть 0;
      выбор "Удаление":
         Вернуть 0;
   }
   Вернуть 0;
}
