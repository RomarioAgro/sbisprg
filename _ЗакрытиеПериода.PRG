Функция _ЗакрытиеПериода()
{
#   оЗП=Выборка("Закрытые периоды");
#   пДата=01.01.80;
#   Пока(Следующий(оЗП)) пДата=Макс(оЗП.Дата,пДата);
#   пДата++;
#   Если(ДаНет("Закрыть период ?"))
#   Если(ДаНет("Закрывается период на "+пДата+" ?"))
#   {
#      Число пПароль;
#      Спросить("Введите пароль",пПароль);
#      Если(пПароль!=1234)
#      {
#         Сообщить("Период не закрыт !");
#         Вернуть 1;
#      }
#      Очистить(оЗП);
#      оЗП.Дата=пДата;
#      оЗП.Cостояние="Готовится к закрытию (ограничен доступ)";
#      оЗП._Cостояние="Готовится к закрытию";
#      Добавить(оЗП); Очистить(оЗП);
#      Сообщить("Период закрыт !");
#   }
#   Вернуть 1;
}
