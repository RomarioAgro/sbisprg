# обработчик диалога 'Карточка'
функция 'Карточка'(Событие, Поле)
{
# Сообщить(Событие+"\n"+Поле);
# Спросить(Поле);
   ВыборПо(Событие)
   {
      выбор "Инициализация":
         Вернуть "";
      выбор "Изменение":
         Вернуть "";
      выбор "Сохранение":
        Вернуть "";
   }
   Вернуть "";
}